<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body{
            background-color: #f5f5f5;
        }
        div{
            width: 100%;
            height: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }
        p{
            font-size: 20px;
            font-weight: 600;
            color: #000;
        }
    </style>
</head>
<body>

<div>
    <% for(var i=0; i<result.length; i++) {%>
        
       <p>Time That invoice :<%= result[i].createdAt %></p>
       <p>ID:<%= result[i]._id %></p>
       <p>Status<%= result[i].status %></p>
       <p>Total:<%= result[i].total %></p>
       <p>Payment Method<%= result[i].paymentMethod %></p>
    <% } %>
    </ul>

</body>
</html>
